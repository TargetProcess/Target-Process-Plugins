require("../env");var vows=require("vows"),assert=require("assert"),suite=vows.describe("d3.layout.hierarchy");suite.addBatch({hierarchy:{topic:function(){return d3.layout.treemap()},"doesn't overwrite the value of a node that has an empty children array":function(a){var b=a.sticky(!0).nodes({value:1,children:[]});assert.equal(b[0].value,1),a.nodes(b[0]),assert.equal(b[0].value,1)},"a valueless node that has an empty children array gets a value of 0":function(a){var b=a.sticky(!0).nodes({children:[]});assert.equal(b[0].value,0),a.nodes(b[0]),assert.equal(b[0].value,0)}}}),suite.export(module)