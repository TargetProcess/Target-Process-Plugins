define(["Underscore","jQuery","tau/core/extension.base"],function(_,$,ExtensionBase){return ExtensionBase.extend({"bus initialize + $el.ready":function(evt,initConfig,$el){$el.tauProgressIndicator(),$el.tauProgressIndicator("show")},"bus initialize + $el.ready + overview.board.ready":function(evt,initConfig,$el){$el.tauProgressIndicator("hide"),$el.tauProgressIndicator("destroy")},"bus initialize + $el.ready + no.data.in.slice":function(evt,initConfig,$el){$el.tauProgressIndicator("hide"),$el.tauProgressIndicator("destroy")},"bus initialize + $el.ready + view.dom.ready":function(evt,initConfig,$el){var $progress=$el.parent().tauProgressIndicator();this.fire("progressProcess.ready",_.bind($progress.tauProgressIndicator,$progress))},"bus progressProcess.ready:last + before_refresh":function(evt,progress){this.timeout=setTimeout(function(){progress("show")},500)},"bus progressProcess.ready:last + overview.board.ready":function(evt,progress){clearTimeout(this.timeout),progress("hide")},"bus progressProcess.ready:last + no.data.in.slice":function(evt,progress){clearTimeout(this.timeout),progress("hide")},"bus progressProcess.ready:last + destroy":function(evt,progress){progress("destroy")}})})